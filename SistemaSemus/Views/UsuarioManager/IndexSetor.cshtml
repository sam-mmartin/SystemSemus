@model IEnumerable<SistemaSemus.Models.Client.Setor>

@{
    ViewBag.Title = "IndexSetor";
    Layout = "~/Views/Shared/_LayoutBeagle.cshtml";
}

<div class="be-content">
    <div class="main-content container-fluid">
        <div class="user-profile">
            <div class="row">
                <!-- Create -->
                <div class="col-lg-5">
                    <div class="card card-border-color card-border-color-primary">
                        <div class="card-header card-header-divider">Novo Setor<span class="card-subtitle">Insira a descrição do novo setor a ser criado</span></div>
                        <div class="card-body">
                            <form action="/UsuarioManager/CreateSetor" method="post">
                                <div class="form-group pt-2">
                                    <label for="Descricao">Descrição</label>
                                    <input class="form-control" id="Descricao" name="Descricao" type="text" placeholder="Digite a descrição do setor">
                                </div>
                                <div class="row pt-3">
                                    <div class="col-lg-6 pb-4 pb-lg-0">
                                    </div>
                                    <div class="col-sm-6">
                                        <p class="text-right">
                                            <button class="btn btn-space btn-primary" type="submit">Salvar</button>
                                            <button class="btn btn-space btn-secondary" id="cancel">Cancelar</button>
                                        </p>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- Table -->
                <div class="col-lg-7">
                    <div class="widget widget-fullwidth widget-small">
                        <div class="widget-head pb-6">
                            <div class="title">Setores</div>
                        </div>
                        <div class="widget-chart-container">
                            <table class="table table-striped table-hover">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Descrição</th>
                                        <th><i class="icon mdi mdi-info-outline"></i></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@Html.DisplayFor(modelItem => item.ID)</td>
                                            <td>@Html.DisplayFor(modelItem => item.Descricao)</td>
                                            <td>
                                                @Html.ActionLink("Editar", "EditSetor", new { id = item.ID }) |
                                                @Html.ActionLink("Detalhes", "DetailsSetor", new { id = item.ID }) |
                                                @Html.ActionLink("Apagar", "DeleteSetor", new { id = item.ID })
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
